class Solution {
    public boolean canAliceWin(int n) {
        int x = 10;
        int moves = 0;
        while (n >= x) {
            n -= x;
            x--;
            moves++;
        }
        return moves % 2 == 1;
    }
}